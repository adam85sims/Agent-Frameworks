{
  "entry_points": [
    "src/main.py"
  ],
  "key_components": {
    "core": "src/core/",
    "tests": "tests/"
  },
  "database_schema": {
    "main": {
      "db_path": "data/app.db",
      "tables": [
        "example_table (id PRIMARY KEY, data TEXT, created_at TIMESTAMP)"
      ]
    }
  },
  "api_endpoints": {
    "base_url": "http://localhost:8000",
    "routes": [
      "GET /health",
      "POST /api/v1/data"
    ]
  }
}
